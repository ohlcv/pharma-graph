# 问题清单与修复追踪

> 把代码体检结果转为可勾选的修复任务。按优先级排序，逐项推进。

---

## P0 — 必须先修

- [x] **1. 加 ESLint + Prettier 配置** — 仓库目前完全没有 `.eslintrc` / `.prettierrc` / `.editorconfig`，但代码里已经在用 `// eslint-disable-next-line`
- [x] **2. 给纯逻辑函数加单测** — `buildGraph`、`parseFrontmatter`、`extractSectionNumber` (tour.ts)、`TourEngine` 拓扑排序都是绝佳测试目标，目前零测试

## P1 — 重要问题

### 架构 / 上帝模块
- [x] **4. `src/ui/main.ts` 拆模块** — 528 行承担了 boot、布局菜单、调试面板、搜索、resize、键盘、音乐播放器等；`exposeGlobals` 把 18+ 函数挂 `window`
- [x] **5. 把 `window.x = ...` 全局污染改成事件委托** — 改一个按钮要改 HTML inline handler + TS 暴露 + 函数实现 3 处；用 `data-action="..."` + delegated listener 统一
- [ ] **6. `state.ts` 跟 `UiToggle` 双源状态合并** — `uiState.isPanelPinned`、`uiState.tourBarCollapsed` 是影子值，真值在 `UiToggle.value`，两份容易不同步

### 代码质量
- [ ] **7. `app-debug.ts:356` 死函数** — `prevNodeName` 永远返回 null，注释承认但调用方还在用
- [ ] **8. `detail-panel.ts` 的 SKIP 字符串匹配改为 hash/flag** — 当前匹配"它在整套框架里属于哪一层、放在哪一块？"字符串，用户写了同名章节会被误删
- [ ] **9. 三处边类型定义合并到 `config.ts` 单一源** — `validate.ts` 的 `VALID_EDGE_TYPES`、`config.ts` 的 `EDGE_TYPE_LABEL`、`frontmatter.md` 文档分散维护
- [ ] **10. 清理 `as any` 逃生舱** — `renderer.ts` 2 处、`main.ts` 22 处 `(window as any).xxx`；为节点数据写 `NodeDataDefinition` 接口

### 潜在 Bug
- [x] **11. 修复 `boot()` 时序 bug** — `initGraphEvents` 在 `tourController` 赋值前调用闭包，第一帧 tap 会炸 `Cannot read property 'isRunning' of undefined`
- [x] **12. 漫游控制双触发** — `TourController` 已用事件委托，但 `main.ts` 又把 `tourStop()` 等暴露给 `window`，按钮被绑两次
- [ ] **13. `_dbg.overlay()` 不同步按钮 DOM 状态** — API 翻转 `debugOverlayActive` 但不触发按钮 click 监听器，两条路径行为不一致
- [ ] **14. `parseFrontmatter` 静默丢边 vs CLI 报错行为不一致** — 浏览器里 target 为空字符串的边被静默丢弃，CLI 校验里会报错；两边行为不一致
- [ ] **15. `tour.ts` 的 `totalExplored` 在删节点时不更新** — 漫游过程中删节点后这个数字会撒谎
- [ ] **16. `TourController` `_restartAttempts` 3 次后无 UI 提示** — 用户视角看到"漫游突然停了"，没有解释
- [ ] **17. `randomize` 使用 `cy.width()/cy.height()` 是 viewport 尺寸** — 用户切换桌面↔手机会乱套

### 死代码 / 文档
- [ ] **18. 处理 `content-manager.ts` / `markdown-parser.ts` 死代码** — `scanContentDir` 只在 `validate.ts` 用；`markdown-parser.ts` 功能已被 `frontmatter.ts` 覆盖
- [ ] **19. `Renderer` 职责越界拆出去** — `getEdgeReason`、`getEdgeMidpoint`、`setDragMode` 不属于渲染器
- [ ] **20. 修正 `audit-frontmatter.ts` 在 README 缺说明** — 24KB 脚本，新人不知何时用

## P2 — 用户价值 / 性能

- [x] **21. 图谱导出 PNG/SVG** — `DEVELOP.md` 第 354 行 P2 任务未实现（删除`DEVELOP.md`相关内容，不需要这个功能）
- [ ] **22. 按 `location` 章节树筛选 UI** — frontmatter 有完整 location 字段但侧栏未暴露（先不用）
- [ ] **23. 223 个并行 fetch 合并为 1-3 个** — 浏览器并发上限通常 6，部署到生产会排队（讨论后决定）
- [x] **24. 顶部轮播词库跟当前学科联动** — 选中药学节点 → 轮播显示中药学术语（先不用）
- [ ] **25. `forensic panel` 性能优化** — `buildRulesTable` 给所有节点生成 HTML，223 节点 × 5 次点击 = 卡顿

## P3 — 长期

- [x] **26. i18n** — 全站硬编码中文，没有英文界面选项（先不管）
- [ ] **27. 多图谱切换** — 目前硬编码"3 本书"，加图谱要改 carousel + manifest（讨论后决定）
- [ ] **28. 节点编辑** — `DEVELOP.md` P3 任务；能在图谱里直接改 frontmatter
- [ ] **29. ARIA 补全** — 节点详情面板 collapse 按钮、tour 折叠 handle、搜索结果导航缺 `aria-expanded` / `aria-live`
- [ ] **30. 词库分层** — `vocabulary.ts` 31KB 含佛学/瑜伽等，药学只 30 术语；考虑拆 `vocabulary-pharma.ts`（先不要）
- [ ] **31. 开源准备** — 移除调试面板（或 DEV-only）、清掉 `console.info` 诊断、移除硬编码音乐、移除 `window._dbg` 暴露

---

## 进度

- 总任务：**31** → **30**（archive 是刻意保留）
- 已完成：**7 / 30** ✅（P0 × 2 + P1 #4, #5, #11, #12 + P2 #21 删功能）
- 正在做：—
